// //Declare a variable called username.
var username;

// // Declare a variable called myName & assign to it a string that represents your Full Name.
username = "Amin Hussain";

// // 3. Write a script to
// // a. declare a JS variable, titled message
// // b. assign “Hello World” to the variable message
// // c. display the message in alert box

var message;
message = "Hello World";

alert(message);

// 4. Write a script to save student’s bio data in JS variables and show the data in alert boxes.

var studentName = "Amin Hussain";
var studentID = 1069;
var studentEmail = "amin@abc.com";
var courseName = "Mobile & Cloud Computing, Module A";
var age = 17;

alert(studentID);
alert(studentName);
alert(studentEmail);

var alertBox = "Pizza \nPizz \nPiz \nPi";
alert(alertBox);

// Store the following into variables: number of children,partner’s name, geographic location, and job title.

var jobTitle = "Web Developer";
var partnerName = "Marry";
var geoLocation = "Karachi";
var numofChildren = 4;

alert(
  `You will be a ${jobTitle} in ${geoLocation}, and married to ${partnerName} with ${numofChildren} kids`
);

//  Declare a variable called email & assign to it a string that represents your Email Address (e.g. abc@xyz.com). Show the below mentioned message in an alert box. (Hint: use string concatenation)

var userEmail = "amin@abc.com";

alert("My Email Address is " + userEmail);

// 8. Declare a variable called book & give it the value “A smarter way to learn JavaScript”. Display the following message in an alert box:

var newBook = "A smarter way to learn JavaScript";

alert(newBook);

// 9. Use the variables, declared in exercise 4 & show the details giving meaning to data. Like “My name is Ali Khan”, “I am 17 years old”, “I study in SSUET”, “I take Mobile & Cloud Computing, Module A classes”

console.log(
  `This is my ID ${studentID} and my name is ${studentName}, I am ${age} years old and I take ${courseName} classes`
);

// 10. Write a script to display this in browser through JS

document.write("Yay! I can write HTML code using JS");

// 9. Store following string in a variable and show in alert and browser through JS
var speacialString = "“▬▬▬▬▬▬▬▬▬ஜ۩۞۩ஜ▬▬▬▬▬▬▬▬▬”";
alert(speacialString);
